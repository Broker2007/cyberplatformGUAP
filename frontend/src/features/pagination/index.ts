import { Pagination } from './ui/Pagination';
import { paginationActions, paginationReducer } from './model/slice/paginationSlice';
import { useDebounce } from './model/hooks/useDebounce';

export {
    Pagination, paginationActions, paginationReducer, useDebounce,
};
